{
  "name": "ubf",
  "version": "0.0.1",
  "description": "Universal Binary Format",
  "author": "Stefan Rumzucker",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/stefanr/node-ubf.git"
  },
  "main": "dist/cjs/index",
  "scripts": {
    "build": "npm --prefix build run build",
    "build-install": "npm --prefix build install",
    "test": "npm --prefix test test",
    "test-install": "npm --prefix test install"
  },
  "dependencies": {
    "node-int64": "^0.4.0",
    "yaee": "^0.1.0"
  },
  "devDependencies": {
    "babel-core": "^5.8.0"
  },
  "license": "MIT"
}
